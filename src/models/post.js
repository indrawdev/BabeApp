class Post {
    constructor(category, id, ownerId, title) {
        this.category = category;
        this.id = id;
        this.ownerId = ownerId;
        this.title = title;
    }
}

export default Post