class Post {
    constructor(id, catId, ownerId, title, cityId) {
        this.id = id;
        this.catId = catId;
        this.ownerId = ownerId;
        this.title = title;
        this.cityId = cityId;
    }
}

export default Post